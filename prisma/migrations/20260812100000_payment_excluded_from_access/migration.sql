-- 1) Payment.excludedFromAccess — гроші прийшли, але система свідомо НЕ зарахувала їх
--    у доступ (orphan-списання по закритій підписці, списання понад ліміт місяців,
--    розбіжність суми). Без цієї позначки такий платіж рахувався як повноцінний
--    оплачений місяць у `calculateAccessUntil` — тобто відмова продовжити доступ
--    відкочувалась сама собою при наступному перерахунку.
-- 2) Payment.promoCodeId — під який глобальний промокод ми інкрементнули лічильник.
--    Потрібен, щоб Declined/Expired повернув використання назад у ліміт (і щоб це
--    було ідемпотентно: після повернення поле обнуляється).
-- Обидві колонки nullable/з дефолтом і IF NOT EXISTS — безпечно застосовувати повторно.
ALTER TABLE "Payment" ADD COLUMN IF NOT EXISTS "excludedFromAccess" BOOLEAN NOT NULL DEFAULT false;
ALTER TABLE "Payment" ADD COLUMN IF NOT EXISTS "promoCodeId" TEXT;

-- Backfill НЕ робимо: історичні orphan-платежі лишаються зарахованими, бо доступ по них
-- уже перерахований і зміна заднім числом обірвала б людям доступ без рішення менеджера.
