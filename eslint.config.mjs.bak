// eslint.config.mjs
import { defineConfig, globalIgnores } from 'eslint/config';
import nextVitals from 'eslint-config-next/core-web-vitals';

const eslintConfig = defineConfig([
  ...nextVitals,
  {
    rules: {
      // Вимикаємо правило для неекранованих лапок (')
      'react/no-unescaped-entities': 'off',
      // Вимикаємо правило для невикористаних змінних (попередження, але не помилка)
      '@typescript-eslint/no-unused-vars': 'warn',
      // Вимикаємо правило для 'any' (попередження, але не помилка)
      '@typescript-eslint/no-explicit-any': 'warn',
    },
  },
  // Ігноруємо стандартні папки Next.js
  globalIgnores(['.next/**', 'out/**', 'build/**', 'next-env.d.ts']),
]);

export default eslintConfig;